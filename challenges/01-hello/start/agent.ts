import { generateText } from 'ai';
import { model } from '../../../shared/model';

async function main() {
  // TODO 1: Call generateText() with the model and ask it for a joke.
  // Store the result and print the text using console.log()
}

main();
