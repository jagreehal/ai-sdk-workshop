import { generateText } from 'ai';
import { model } from '../../../shared/model';
import * as readline from 'node:readline/promises';

async function main() {
  // TODO 1: Create a readline interface to ask for user input
  // const terminal = readline.createInterface({
  //   input: process.stdin,
  //   output: process.stdout,
  // });

  // TODO 2: Ask the user for a topic, call generateText() with a prompt that includes
  // the topic, then print the result. Don't forget to close the terminal when done.
}

main();
